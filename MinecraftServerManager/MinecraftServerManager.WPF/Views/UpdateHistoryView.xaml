<UserControl x:Class="MinecraftServerManager.WPF.Views.UpdateHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Update History" FontSize="24" FontWeight="Bold" Margin="10"/>

        <!-- Toolbar -->
        <ToolBar Grid.Row="1" Padding="5">
            <Button Content="Refresh" Command="{Binding RefreshHistoryCommand}" Padding="10,5" ToolTip="Refresh history"/>
            <Separator/>
            <TextBlock Text="Total Updates:" VerticalAlignment="Center" Margin="10,0,5,0"/>
            <TextBlock Text="{Binding EntryCount}" VerticalAlignment="Center" FontWeight="Bold"/>
        </ToolBar>

        <!-- History DataGrid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding HistoryEntries}"
                  SelectedItem="{Binding SelectedEntry}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  Margin="10"
                  AlternatingRowBackground="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="150"/>
                <DataGridTextColumn Header="First Updated" Binding="{Binding FirstUpdatedAt}" Width="*"/>
                <DataGridTextColumn Header="Last Updated" Binding="{Binding LastUpdatedAt}" Width="*"/>
                <DataGridTextColumn Header="Times Updated" Binding="{Binding TimesUpdated}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
