<UserControl x:Class="MinecraftServerManager.WPF.Views.ServerListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:converters="clr-namespace:MinecraftServerManager.WPF.Converters"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:ServerStatusToColorConverter x:Key="StatusToColor"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Toolbar -->
            <RowDefinition Height="*"/>
            <!-- DataGrid -->
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <ToolBar Grid.Row="0" Padding="5">
            <Button Content="Refresh" Command="{Binding RefreshServersCommand}" Padding="10,5" ToolTip="Refresh server list"/>
            <Separator/>
            <Button Content="Start All" Command="{Binding StartAllServersCommand}" Padding="10,5" ToolTip="Start all stopped servers"/>
            <Button Content="Stop All" Command="{Binding StopAllServersCommand}" Padding="10,5" ToolTip="Stop all running servers"/>
            <Separator/>
            <TextBlock Text="Servers:" VerticalAlignment="Center" Margin="10,0,5,0"/>
            <TextBlock Text="{Binding ServerCount}" VerticalAlignment="Center" FontWeight="Bold"/>
            <ui:ProgressRing IsActive="True" Width="20" Height="20" Margin="15,0,0,0" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}"/>
        </ToolBar>

        <!-- Server DataGrid -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Servers}"
                  SelectedItem="{Binding SelectedServer}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  AlternatingRowBackground="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}">

            <DataGrid.Columns>
                <!-- Status Indicator -->
                <DataGridTemplateColumn Header="Status" Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Ellipse Width="12" Height="12"
                                     Fill="{Binding StatusColor}"
                                     ToolTip="{Binding IsRunning, StringFormat='Running: {0}'}"
                                     HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Server Name -->
                <DataGridTextColumn Header="Server Name" Binding="{Binding Name}" Width="*" MinWidth="150"/>

                <!-- Version -->
                <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="120"/>

                <!-- Server Name from Properties -->
                <DataGridTextColumn Header="Display Name" Binding="{Binding ServerName}" Width="*" MinWidth="150"/>

                <!-- Port -->
                <DataGridTextColumn Header="Port" Binding="{Binding Port}" Width="80"/>

                <!-- Gamemode -->
                <DataGridTextColumn Header="Gamemode" Binding="{Binding Gamemode}" Width="100"/>

                <!-- Max Players -->
                <DataGridTextColumn Header="Max Players" Binding="{Binding MaxPlayers}" Width="100"/>
            </DataGrid.Columns>

            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Start Server" Command="{Binding SelectedServer.StartServerCommand}"/>
                    <MenuItem Header="Stop Server" Command="{Binding SelectedServer.StopServerCommand}"/>
                    <Separator/>
                    <MenuItem Header="Open Folder" Command="{Binding SelectedServer.OpenFolderCommand}"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>
    </Grid>
</UserControl>
